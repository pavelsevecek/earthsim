#version 430 core
layout(local_size_x = 128) in;
layout(std430,binding=2) buffer TerrainFlow { ivec4 terrainFlow[]; };
uniform uint capacity;

void main() {
    uint id=gl_GlobalInvocationID.x;
    if(id<capacity) terrainFlow[id]=ivec4(0);
}
